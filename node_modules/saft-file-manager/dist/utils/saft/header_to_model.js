"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.HeaderToModel = void 0;
const SAFTHeader_1 = require("../../validations/model/saft/header/SAFTHeader");
class HeaderToModel {
    constructor({ headerMap }) {
        this.headerModel = new SAFTHeader_1.SAFTHeader();
        this.headerMap = headerMap;
    }
    get() {
        let headerEntries = Object.fromEntries(this.headerMap);
        let headerStr = JSON.stringify(headerEntries);
        this.headerModel = JSON.parse(headerStr);
        let addressMap = this.headerMap.get('companyAddress');
        if (typeof (addressMap) !== 'undefined') {
            let addressEntries = Object.fromEntries(addressMap);
            let addressStr = JSON.stringify(addressEntries);
            let addressModel = JSON.parse(addressStr);
            this.headerModel.companyAddress = addressModel;
        }
        return this.headerModel;
    }
}
exports.HeaderToModel = HeaderToModel;
