import * as fs from 'fs';
import { SaftFileParser } from '../../src/domain/SaftFileParser';
import { SaftFileParserImpl } from '../../src/domain/SaftFileParserImpl';

test('Tell me who you are', async () => {
  /*
    let content = fs.readFileSync("test.xml");
    const dataView = new Uint8Array(content);
    let parser:SaftFileParser= new SaftFileParserImpl();
    expect( await parser.parse(dataView)).toBe(true)

    */
});
